/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you want to enable internationalized routing, you can extend this.
  // i18n: {
  //   locales: ['fr', 'ar'],
  //   defaultLocale: 'fr',
  // },
};

module.exports = nextConfig;